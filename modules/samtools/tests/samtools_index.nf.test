nextflow_process {

    name "Test Process SAMTOOLS_INDEX"
    script "../samtools_index.nf"
    process "SAMTOOLS_INDEX"

    test("Should run index reads_father.bam correctly and create bai file") {

        when {
            params {
                outdir = "./results"
            }
            process {
                """
                input[0] = file("${projectDir}/data/bam/reads_father.bam")
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

    test("Should run index reads_mother.bam correctly and create bai file") {

        when {
            params {
                outdir = "./results"
            }
            process {
                """
                input[0] = file("${projectDir}/data/bam/reads_mother.bam")
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

    test("Should run index reads_son.bam correctly and create bai file") {

        when {
            params {
                outdir = "./results"
            }
            process {
                """
                input[0] = file("${projectDir}/data/bam/reads_son.bam")
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
